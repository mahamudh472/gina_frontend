import MeditationLayout from "@/components/MeditationLayout";

export default function MeditationRootLayout({ children }: { children: React.ReactNode }) {
  return <MeditationLayout>{children}</MeditationLayout>;
}
